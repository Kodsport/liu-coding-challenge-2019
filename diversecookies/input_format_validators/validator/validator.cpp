#include "validator.h"

const int MX_N = 1000000000;
const int MX_ABC = 1000000000;

void run() {
  Int(2, MX_N);
  Space();
  Int(0, MX_ABC);
  Space();
  Int(0, MX_ABC);
  Space();
  Int(0, MX_ABC);
  Endl();
  Eof();
}
